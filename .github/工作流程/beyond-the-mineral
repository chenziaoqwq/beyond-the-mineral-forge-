# This is a basic workflow to help you get started with Actions

姓名:beyond_the_mineral

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
- name: Java JDK installation
Purpose: the actions/setup-java@v2.3.0
And:
# to set up the Java version. Need a complete or semver Java version. To view the README file support syntax examples
Java v1ersion:16.0.2
# Java release. In the file view support distribution list
Distribution:
# package type (JDK and jre, JDK + fx, jre + fx)
Java - package: # optional, default to the JDK
# package of architecture
Original: # optional, x64 on by default
# of compressed the JDK's path
JdkFile: # option
# if you want to operate in conformity with the latest version of the standard version, please set this option
Look at the latest: # option
# pom. XML files distributionManagement repository ID. The default for ` making `
Server - id: # option, the default is dead simple
# for Apache Maven repository to authenticate the user name of the environment variable name. The default is $GITHUB_ACTOR
Server - the username: # optional, default to GITHUB_ACTOR
# for the Apache Maven repository for authentication password or token environment variable name. The default is $GITHUB_TOKEN
Server - password: # optional, default to GITHUB_TOKEN
# will write Settings. The path of the XML file. The default value is ~ / m2.
Set the path: # option
# override Settings. The XML file (if any). The default value is "true".
Override Settings: # option, the default is true
# to import GPG module. The default is empty string.
GPG -- private key: # option
In the name of the environment variable # GPG module code. The default value is $GPG_PASSPHRASE.
GPG - password: # option
# used for the name of the build platform dependencies. It can be "maven" or "gradle.
View: # option
# job status could be dynamically transferred to publish job step solution.
Job status: # option, the default value is ${{job. Status}}
© 2021 GitHu

